{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <h1 class="text-center">Category creation</h1>

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button  class="btn bg-info" type="submit" value="Create" >Create </button>
            {{ form_end(form) }}
        </div>

<div class="row">
    <ul class="list-inline">
        <li>
            <a href="{{ path('category_index') }}">Back to the list</a>
        </li>
    </ul>
</div>

    </div>
    <
{% endblock %}
